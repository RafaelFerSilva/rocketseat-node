// Buffer é a representação de um espaço na memória do computador usado para transitar dados de forma rápido (Salvar e ler dados de forma performática)
const buf = Buffer.from("hello")
console.log(buf.toJSON())